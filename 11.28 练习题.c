#include <stdio.h>
#include <string.h>
int main()
{
	/*char c[6];int i=0;
	for (i=0;i<6;c[i]=getchar(),i++);
	for (i=0;i<6;i++)
	putchar (c[i]);					//第八题 
	*/
	
	/*char a[20]="AB",b[20]="LMNP";
	int i=0;
	strcat (a,b);
	while (a[i++]!='\0')
	b[i]=a[i];
	puts(b);						//第十题 
	*/
	
	
	/*char s[80],c='a';
	int i=0;
	scanf("%s",s);
	while (s[i]!='\0')
	{
	if (s[i]==c) s[i]=s[i]-32;
	else if (s[i]==c-32)
	s[i]=s[i]+32;
	i++;
	}								//第十一题，将输入的字符串里的a,A相互转换，其他字符不变，遇空格停止输出 
	puts(s);*/
	
	
	/*char a[20];
	int i=0;
	scanf("%s",&a);
	while (a[i]!='\0')
	{
		if (a[i]==" ")
	}
	*/	
	
 } 
